#include <bits/stdc++.h>
using namespace std;

int main() {
	int x,y ; cin >> x >> y;
    if (x==0) cout << y;
    else if (y==0) cout << x;
    else {
   int p = x+(y*10); cout << p;
        }
        return 0;
}

//https://www.codechef.com/START173D/problems/WAPEN
